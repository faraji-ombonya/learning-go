# Sharing Functions in the Same Module

You can access functions in the same module but different files without importing.
All you need to do is run the module.

Note:
A common beginner mistake is running the main file alone, hence `item undefined` errors.